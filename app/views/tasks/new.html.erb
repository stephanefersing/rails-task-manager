<h1>Create a task</h1>

<%= form_for (@task) do |f|%>
<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :details %>
<%= f.text_field :details %>

<%= f.submit "add a task !" %>
<% end %>

<p><%=link_to "back to list", tasks_path%></p>
