<h1>Edit task</h1>

<%= form_for (@task) do |f|%>
<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :details %>
<%= f.text_field :details %>

<%= f.check_box :completed %>

<%= f.submit "Edit task !" %>
<% end %>

<p><%=link_to "back to list", tasks_path%></p>
